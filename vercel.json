{
  "version": 2,
  "buildCommand": "yarn install && yarn build:verbose",
  "builds": [
    {
      "src": "dist/ssr/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/ssr/server.js"
    }
  ],
  "env": {
    "DEBUG": "*"
  }
}
