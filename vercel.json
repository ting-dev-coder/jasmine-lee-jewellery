{
  "version": 2,
  "buildCommand": "yarn install && npx quasar prepare && yarn build:verbose",
  "outputDirectory": "dist/ssr",
  "builds": [
    {
      "src": "dist/ssr/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/ssr/server.js"
    }
  ],
  "env": {
    "DEBUG": "*"
  }
}
